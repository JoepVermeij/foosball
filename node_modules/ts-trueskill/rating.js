"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var mathematics_1 = require("./mathematics");
var trueskill_1 = require("./trueskill");
/**
 * The default mu and sigma value follows the global environment's settings.
 * If you don't want to use the global, use `TrueSkill.createRating` to
 * create the rating object.
 */
var Rating = /** @class */ (function (_super) {
    __extends(Rating, _super);
    function Rating(mu, sigma) {
        var _this = this;
        if (Array.isArray(mu)) {
            mu = mu[0], sigma = mu[1];
        }
        else if (mu instanceof mathematics_1.Gaussian) {
            sigma = mu.sigma;
            mu = mu.mu;
        }
        if (!mu) {
            mu = trueskill_1.global_env().mu;
        }
        if (!sigma) {
            sigma = trueskill_1.global_env().sigma;
        }
        _this = _super.call(this, mu, sigma) || this;
        return _this;
    }
    Rating.prototype.toString = function () {
        var mu = this.mu.toFixed(3);
        var sigma = this.sigma.toFixed(3);
        return "Rating(mu=" + mu + ", sigma=" + sigma + ")";
    };
    return Rating;
}(mathematics_1.Gaussian));
exports.Rating = Rating;
//# sourceMappingURL=rating.js.map